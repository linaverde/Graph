#include <iostream>
#include "Graph.h"

int main() {
    /*Graph g;
    for (int i = 1; i < 10; i++){
        g.addPoint( "x" + std::to_string(i));
    }
    g.addEdge("x1", "x2", -3);
    g.addEdge("x1", "x5", 2);

    g.addEdge("x2", "x3", -5);
    g.addEdge("x2", "x4", 15);
    g.addEdge("x2", "x5", 12);

    g.addEdge("x3", "x8", 24);
    g.addEdge("x3", "x4", 8);

    g.addEdge("x4", "x1", 6);
    g.addEdge("x4", "x6", 18);
    g.addEdge("x4", "x7", 4);
    g.addEdge("x4", "x9", 11);
    g.addEdge("x4", "x3", 8);

    g.addEdge("x5", "x4", -7);
    g.addEdge("x5", "x2", 12);
    g.addEdge("x5", "x6", 20);

    g.addEdge("x6", "x5", 20);
    g.addEdge("x6", "x7", 9);

    g.addEdge("x7", "x3", -10);
    g.addEdge("x7", "x4", 4);
    g.addEdge("x7", "x8", 16);
    g.addEdge("x7", "x6", 9);

    g.addEdge("x8", "x3", 24);
    g.addEdge("x8", "x7", 16);
    g.addEdge("x8", "x9", 22);

    g.addEdge("x9", "x4", 11);
    g.addEdge("x9", "x6", -13);


    //g.printConnections();

    //g.FordMyrrBelman("x1");

    g.DFS("x1");*/

    Graph *g = new Graph();



    return 0;
}